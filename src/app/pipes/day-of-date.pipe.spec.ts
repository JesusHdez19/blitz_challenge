import { DayOfDatePipe } from './day-of-date.pipe';

describe('DayOfDatePipe', () => {
  it('create an instance', () => {
    const pipe = new DayOfDatePipe();
    expect(pipe).toBeTruthy();
  });
});
