import { MathRoundNumberPipe } from './math-round-number.pipe';

describe('MathRoundNumberPipe', () => {
  it('create an instance', () => {
    const pipe = new MathRoundNumberPipe();
    expect(pipe).toBeTruthy();
  });
});
